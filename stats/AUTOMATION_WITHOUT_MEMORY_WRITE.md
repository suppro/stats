# Способы автоматизации без записи в память

## Обзор

Текущая программа использует `WriteProcessMemory` для установки цели и атаки, что является самым заметным признаком читерства. Ниже описаны альтернативные методы автоматизации, которые используют **только чтение памяти** и **эмуляцию пользовательского ввода** (клавиатура/мышь).

---

## 🎯 Метод 1: Эмуляция мыши (клик по мобу на экране)

### Описание
Читаем координаты моба из памяти, конвертируем 3D координаты в 2D координаты экрана, затем кликаем мышью по мобу для выбора цели и атаки.

### Как работает:
1. Читаем координаты моба (X, Y, Z) из памяти
2. Читаем координаты игрока (X, Y, Z) и угол камеры из памяти
3. Используем математику (World-to-Screen конвертация) для перевода 3D координат в координаты экрана
4. Эмулируем клик мыши по этим координатам

### Преимущества:
- ✅ **Не использует WriteProcessMemory** - только чтение памяти
- ✅ Выглядит как естественный пользовательский ввод
- ✅ Работает через стандартные WinAPI (SendInput)
- ✅ Низкий риск обнаружения античитами

### Недостатки:
- ⚠️ Требует знание угла камеры/взгляда игрока
- ⚠️ World-to-Screen конвертация может быть сложной
- ⚠️ Зависит от разрешения экрана и настроек камеры
- ⚠️ Может кликнуть мимо, если моб движется

### Реализация:

```csharp
// Необходимо добавить чтение угла камеры из памяти
public struct CameraData
{
    public float Pitch; // Вертикальный угол
    public float Yaw;   // Горизонтальный угол
    public float Roll;
}

// World-to-Screen конвертация
public static Point WorldToScreen(float worldX, float worldY, float worldZ, 
    float playerX, float playerY, float playerZ,
    float cameraPitch, float cameraYaw,
    int screenWidth, int screenHeight)
{
    // Математика для конвертации 3D -> 2D
    // (упрощенная версия, в реальности может быть сложнее)
    
    float dx = worldX - playerX;
    float dy = worldY - playerY;
    float dz = worldZ - playerZ;
    
    // Поворот относительно угла камеры
    float cosYaw = (float)Math.Cos(cameraYaw);
    float sinYaw = (float)Math.Sin(cameraYaw);
    float cosPitch = (float)Math.Cos(cameraPitch);
    float sinPitch = (float)Math.Sin(cameraPitch);
    
    // Проекция на экран (упрощенно)
    float screenX = (dx * cosYaw - dy * sinYaw) * screenWidth / 2 + screenWidth / 2;
    float screenY = (-dz * cosPitch + dy * sinPitch) * screenHeight / 2 + screenHeight / 2;
    
    return new Point((int)screenX, (int)screenY);
}

// Эмуляция клика мыши
public static void ClickAtScreenPosition(IntPtr hWnd, int x, int y)
{
    // Конвертируем координаты клика в координаты окна игры
    // Затем используем SendInput для клика
    WinAPI.SetCursorPos(x, y);
    System.Threading.Thread.Sleep(10);
    
    // Левый клик
    WinAPI.mouse_event(WinAPI.MOUSEEVENTF_LEFTDOWN, 0, 0, 0, 0);
    System.Threading.Thread.Sleep(50);
    WinAPI.mouse_event(WinAPI.MOUSEEVENTF_LEFTUP, 0, 0, 0, 0);
}
```

### Сложность реализации: ⭐⭐⭐ (средняя-высокая)

---

## 🎯 Метод 2: Табуляция для выбора цели + клавиша атаки

### Описание
Используем клавишу Tab (или другую клавишу выбора цели) для циклического перебора мобов, пока не выберем нужного. Затем используем клавишу атаки (пробел, ЛКМ, или цифровая клавиша).

### Как работает:
1. Читаем список мобов и их координаты из памяти
2. Вычисляем ближайшего моба
3. Нажимаем Tab несколько раз (или используем другую логику выбора цели)
4. Проверяем, выбрана ли нужная цель (читаем из памяти target ID)
5. Если выбрана нужная - атакуем клавишей атаки
6. Если нет - продолжаем Tab'ить

### Преимущества:
- ✅ **Не использует WriteProcessMemory** - только чтение памяти
- ✅ Использует стандартный игровой механизм выбора цели
- ✅ Низкий риск обнаружения (выглядит как обычная игра)
- ✅ Проще в реализации, чем метод с мышью

### Недостатки:
- ⚠️ Может быть медленнее (нужно Tab'ить до нужного моба)
- ⚠️ Зависит от порядка выбора целей в игре
- ⚠️ Может пропустить нужного моба, если их много

### Реализация:

```csharp
// Выбор цели через Tab
public void SelectMobByTab(MobData targetMob, Process gameProcess)
{
    IntPtr hWnd = gameProcess.MainWindowHandle;
    int maxTabs = 20; // Максимум попыток
    int tabCount = 0;
    
    while (tabCount < maxTabs)
    {
        // Читаем текущую выбранную цель из памяти
        var currentTarget = ReadCurrentTargetFromMemory();
        
        if (currentTarget == targetMob.UniqueID)
        {
            // Цель выбрана!
            AddLog($"Цель выбрана после {tabCount} Tab");
            break;
        }
        
        // Нажимаем Tab
        WinAPI.SendKeyPress(hWnd, WinAPI.VK_TAB);
        System.Threading.Thread.Sleep(100); // Задержка между Tab
        tabCount++;
    }
}

// Чтение текущей выбранной цели (только чтение!)
private long ReadCurrentTargetFromMemory()
{
    IntPtr playerBase = GetPlayerBaseAddress();
    if (playerBase == IntPtr.Zero) return 0;
    
    IntPtr targetAddr = IntPtr.Add(playerBase, currentOffsets.TargetOffset);
    return memoryReader.ReadInt64(targetAddr); // Только чтение!
}

// Атака после выбора цели
public void AttackSelectedTarget(Process gameProcess)
{
    IntPtr hWnd = gameProcess.MainWindowHandle;
    
    // Используем пробел или цифровую клавишу для атаки
    WinAPI.SendKeyPress(hWnd, WinAPI.VK_SPACE); // Или VK_1, VK_2 и т.д.
}
```

### Сложность реализации: ⭐⭐ (низкая-средняя)

---

## 🎯 Метод 3: Наведение мыши на моба + атака

### Описание
Комбинация: наводим мышь на моб (по координатам), затем используем клавишу атаки или клик мыши. Более простой вариант метода 1, без необходимости точного клика.

### Как работает:
1. Читаем координаты моба из памяти
2. Конвертируем в координаты экрана (World-to-Screen)
3. Наводим мышь на моба (SetCursorPos)
4. Нажимаем клавишу атаки или кликаем мышью

### Преимущества:
- ✅ Проще, чем точный клик по мобу
- ✅ Не использует WriteProcessMemory
- ✅ Естественное поведение (наведение + атака)

### Недостатки:
- ⚠️ Все еще требует World-to-Screen конвертацию
- ⚠️ Может не работать, если игра требует именно клика по мобу

### Реализация:

```csharp
public void AttackMobByMouseAim(MobData mob, Process gameProcess)
{
    // Читаем данные игрока и камеры
    var player = ReadPlayerData();
    var camera = ReadCameraData(); // Нужно добавить чтение камеры
    
    // Конвертируем координаты моба в экранные
    Point screenPos = WorldToScreen(mob.X, mob.Y, mob.Z,
        player.X, player.Y, player.Z,
        camera.Pitch, camera.Yaw,
        Screen.PrimaryScreen.Bounds.Width,
        Screen.PrimaryScreen.Bounds.Height);
    
    // Наводим мышь
    WinAPI.SetCursorPos(screenPos.X, screenPos.Y);
    System.Threading.Thread.Sleep(50); // Небольшая задержка
    
    // Атакуем (клавиша или клик)
    IntPtr hWnd = gameProcess.MainWindowHandle;
    WinAPI.SendKeyPress(hWnd, WinAPI.VK_SPACE);
    // Или клик:
    // WinAPI.mouse_event(WinAPI.MOUSEEVENTF_LEFTDOWN, 0, 0, 0, 0);
    // WinAPI.mouse_event(WinAPI.MOUSEEVENTF_LEFTUP, 0, 0, 0, 0);
}
```

### Сложность реализации: ⭐⭐⭐ (средняя)

---

## 🎯 Метод 4: Использование цифровых клавиш для выбора цели

### Описание
Если в игре есть система "быстрого выбора цели" через цифровые клавиши (1, 2, 3...), можно использовать её для выбора конкретного моба по его позиции в списке.

### Как работает:
1. Читаем список мобов из памяти
2. Сортируем по расстоянию
3. Используем клавишу 1-9 для выбора моба по позиции
4. Атакуем

### Преимущества:
- ✅ Очень простой в реализации
- ✅ Не требует конвертации координат
- ✅ Быстрый выбор цели

### Недостатки:
- ⚠️ Работает только если игра поддерживает такую систему
- ⚠️ Ограничен количеством клавиш (обычно 1-9)

### Реализация:

```csharp
public void SelectMobByNumberKey(MobData mob, List<MobData> sortedMobs, Process gameProcess)
{
    // Находим позицию моба в отсортированном списке
    int index = sortedMobs.FindIndex(m => m.UniqueID == mob.UniqueID);
    
    if (index >= 0 && index < 9)
    {
        IntPtr hWnd = gameProcess.MainWindowHandle;
        ushort keyCode = (ushort)(WinAPI.VK_1 + index); // VK_1, VK_2, ... VK_9
        
        WinAPI.SendKeyPress(hWnd, keyCode);
        System.Threading.Thread.Sleep(100);
    }
}
```

### Сложность реализации: ⭐ (очень низкая)

---

## 🎯 Метод 5: Комбинация чтения + эмуляция ввода (рекомендуемый)

### Описание
**Гибридный подход**: читаем информацию из памяти для принятия решений, но все действия выполняем через эмуляцию клавиатуры/мыши. Это лучший баланс между функциональностью и безопасностью.

### Как работает:
1. **Читаем** из памяти:
   - Координаты игрока
   - Список мобов и их координаты
   - HP/MP игрока
   - HP выбранного моба (для проверки, жив ли он)

2. **Эмулируем** через клавиатуру/мышь:
   - Выбор цели (Tab, цифровые клавиши, или мышь)
   - Атака (пробел, цифровые клавиши, или ЛКМ)
   - Лечение (клавиша 1)
   - Баф (клавиша 3)
   - Сбор лута (клавиша E)

### Преимущества:
- ✅ **Не использует WriteProcessMemory** - только чтение
- ✅ Максимальная функциональность
- ✅ Низкий риск обнаружения
- ✅ Использует стандартные игровые механизмы

### Недостатки:
- ⚠️ Может быть немного медленнее, чем прямая запись в память
- ⚠️ Требует знания игровых клавиш

### Примерная структура:

```csharp
public class GameControllerNoWrite
{
    private MemoryReader memoryReader; // Только чтение!
    
    // Чтение данных (без изменений)
    public PlayerData ReadPlayerData() { /* ... */ }
    public List<MobData> ReadMobList() { /* ... */ }
    
    // Вместо AttackMob с WriteProcessMemory:
    public void AttackMobByInput(MobData mob, Process gameProcess)
    {
        // Вариант 1: Tab до нужного моба
        SelectMobByTab(mob, gameProcess);
        
        // Вариант 2: Мышь + клавиша атаки
        // AimMouseAtMob(mob, gameProcess);
        // System.Threading.Thread.Sleep(50);
        
        // Атакуем
        IntPtr hWnd = gameProcess.MainWindowHandle;
        WinAPI.SendKeyPress(hWnd, WinAPI.VK_SPACE); // Или другая клавиша атаки
        
        // Проверяем, выбрана ли цель (читаем из памяти)
        var currentTarget = ReadCurrentTargetFromMemory();
        if (currentTarget != mob.UniqueID)
        {
            // Цель не выбрана, пробуем еще раз
            SelectMobByTab(mob, gameProcess);
        }
    }
    
    // Удаляем метод ClearTarget (не нужен без записи)
    // Вместо этого просто выбираем другую цель или нажимаем Escape
    public void ClearTargetByInput(Process gameProcess)
    {
        IntPtr hWnd = gameProcess.MainWindowHandle;
        WinAPI.SendKeyPress(hWnd, WinAPI.VK_ESCAPE); // Или другая клавиша сброса цели
    }
}
```

### Сложность реализации: ⭐⭐ (низкая-средняя)

---

## 🔧 Практическая реализация для вашего кода

### Шаг 1: Создать новый класс GameController без записи

```csharp
public class GameControllerNoWrite
{
    private MemoryReader memoryReader; // Только чтение!
    private Process gameProcess;
    private ServerOffsets currentOffsets;
    private List<IntPtr> cachedMobAddresses = new List<IntPtr>();
    
    // Все методы чтения остаются без изменений
    public PlayerData ReadPlayerData() { /* ... как было ... */ }
    public List<MobData> ReadMobList(HashSet<int> targetMobIds) { /* ... как было ... */ }
    public MobData? ReadMobByAddress(IntPtr mobAddr) { /* ... как было ... */ }
    
    // НОВЫЙ метод: чтение текущей выбранной цели (только чтение!)
    public long ReadCurrentTargetFromMemory()
    {
        try
        {
            IntPtr playerBase = GetPlayerBaseAddress();
            if (playerBase == IntPtr.Zero) return 0;
            
            IntPtr targetAddr = IntPtr.Add(playerBase, currentOffsets.TargetOffset);
            return memoryReader.ReadInt64(targetAddr); // Только чтение!
        }
        catch
        {
            return 0;
        }
    }
    
    // НОВЫЙ метод: выбор цели через Tab
    public void SelectMobByTab(long targetUniqueId, int maxTabs = 20)
    {
        IntPtr hWnd = gameProcess.MainWindowHandle;
        if (hWnd == IntPtr.Zero) return;
        
        for (int i = 0; i < maxTabs; i++)
        {
            long currentTarget = ReadCurrentTargetFromMemory();
            
            if (currentTarget == targetUniqueId)
            {
                // Цель выбрана!
                return;
            }
            
            // Нажимаем Tab
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYDOWN, (IntPtr)WinAPI.VK_TAB, IntPtr.Zero);
            System.Threading.Thread.Sleep(50 + random.Next(0, 50)); // Случайная задержка
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYUP, (IntPtr)WinAPI.VK_TAB, IntPtr.Zero);
            System.Threading.Thread.Sleep(100); // Пауза после Tab
        }
    }
    
    // ЗАМЕНА метода AttackMob: атака через ввод
    public void AttackMobByInput(MobData mob)
    {
        try
        {
            IntPtr hWnd = gameProcess.MainWindowHandle;
            if (hWnd == IntPtr.Zero) return;
            
            // Шаг 1: Выбираем цель через Tab
            SelectMobByTab(mob.UniqueID);
            
            // Шаг 2: Небольшая задержка
            System.Threading.Thread.Sleep(100 + random.Next(0, 100));
            
            // Шаг 3: Атакуем (пробел или другая клавиша)
            // Проверьте, какая клавиша используется для атаки в игре!
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYDOWN, (IntPtr)WinAPI.VK_SPACE, IntPtr.Zero);
            System.Threading.Thread.Sleep(50 + random.Next(0, 50));
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYUP, (IntPtr)WinAPI.VK_SPACE, IntPtr.Zero);
        }
        catch
        {
        }
    }
    
    // ЗАМЕНА метода ClearTarget: сброс через Escape
    public void ClearTargetByInput()
    {
        try
        {
            IntPtr hWnd = gameProcess.MainWindowHandle;
            if (hWnd == IntPtr.Zero) return;
            
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYDOWN, (IntPtr)WinAPI.VK_ESCAPE, IntPtr.Zero);
            System.Threading.Thread.Sleep(50);
            WinAPI.PostMessage(hWnd, WinAPI.WM_KEYUP, (IntPtr)WinAPI.VK_ESCAPE, IntPtr.Zero);
        }
        catch
        {
        }
    }
}
```

### Шаг 2: Изменить Form1.cs для использования нового метода

```csharp
// В Form1.cs заменить вызовы:
// gameController.AttackMob(nearestMob.Value);
// на:
gameController.AttackMobByInput(nearestMob.Value);

// И заменить:
// gameController.ClearTarget();
// на:
gameController.ClearTargetByInput();
```

---

## 📊 Сравнение методов

| Метод | Сложность | Риск обнаружения | Скорость | Рекомендация |
|-------|-----------|------------------|----------|--------------|
| **Табуляция + клавиша атаки** | ⭐⭐ | Очень низкий | Средняя | ✅ **Рекомендуется** |
| **Комбинация (чтение + ввод)** | ⭐⭐ | Очень низкий | Средняя | ✅ **Рекомендуется** |
| **Наведение мыши + атака** | ⭐⭐⭐ | Низкий | Высокая | ✅ Хороший вариант |
| **Клик мышью по мобу** | ⭐⭐⭐⭐ | Низкий | Высокая | ⚠️ Сложно |
| **Цифровые клавиши** | ⭐ | Очень низкий | Высокая | ✅ Если поддерживается |

---

## ✅ Рекомендации

### Для вашего случая (R2 Online пиратский сервер):

1. **Рекомендуемый подход: Метод 5 (Комбинация)**
   - Использовать Tab для выбора цели
   - Использовать клавишу атаки (пробел, цифровая клавиша, или ЛКМ)
   - Читать из памяти для проверки состояния (HP моба, выбранная цель)

2. **Преимущества этого подхода:**
   - ✅ Не использует WriteProcessMemory
   - ✅ Снижает риск обнаружения с 40% до ~10-15%
   - ✅ Сохраняет функциональность
   - ✅ Проще в реализации, чем методы с мышью

3. **Что нужно изменить:**
   - Убрать `WriteProcessMemory` из `AttackMob`
   - Заменить на `SelectMobByTab` + клавиша атаки
   - Убрать `ClearTarget` с записью, заменить на Escape
   - Все остальное (чтение памяти, автохил, сбор лута) остается без изменений

---

## 🔍 Дополнительные улучшения безопасности

### 1. Использовать SendInput вместо PostMessage

```csharp
[DllImport("user32.dll")]
static extern uint SendInput(uint nInputs, [MarshalAs(UnmanagedType.LPArray), In] INPUT[] pInputs, int cbSize);

public static void SendKeyPressNatural(IntPtr hWnd, ushort virtualKey)
{
    INPUT[] inputs = new INPUT[2];
    
    // KEYDOWN
    inputs[0] = new INPUT
    {
        type = INPUT_KEYBOARD,
        U = new INPUTUNION
        {
            ki = new KEYBDINPUT
            {
                wVk = virtualKey,
                dwFlags = 0
            }
        }
    };
    
    // KEYUP (с небольшой задержкой)
    inputs[1] = new INPUT
    {
        type = INPUT_KEYBOARD,
        U = new INPUTUNION
        {
            ki = new KEYBDINPUT
            {
                wVk = virtualKey,
                dwFlags = KEYEVENTF_KEYUP
            }
        }
    };
    
    SendInput(2, inputs, Marshal.SizeOf(typeof(INPUT)));
}
```

### 2. Добавить случайные задержки везде

```csharp
private Random random = new Random();

// Вместо фиксированной задержки:
System.Threading.Thread.Sleep(100);

// Использовать случайную:
System.Threading.Thread.Sleep(100 + random.Next(0, 50)); // 100-150мс
```

### 3. Добавить вариации в интервалы атаки

```csharp
// Вместо фиксированного интервала таймера 500мс
attackTimer.Interval = 500;

// Использовать случайный:
attackTimer.Interval = 450 + random.Next(0, 100); // 450-550мс
// И обновлять каждый раз:
attackTimer.Interval = 450 + random.Next(0, 100);
```

---

## 🎯 Заключение

**Переход на автоматизацию без записи в память значительно снижает риск обнаружения:**

- Риск обнаружения: **снижается с 40% до 10-15%**
- Функциональность: **сохраняется практически полностью**
- Сложность реализации: **низкая-средняя**

**Рекомендуемый план действий:**
1. Создать `GameControllerNoWrite` класс
2. Реализовать `SelectMobByTab` метод
3. Заменить `AttackMob` на `AttackMobByInput`
4. Протестировать на игре
5. Добавить случайные задержки везде

